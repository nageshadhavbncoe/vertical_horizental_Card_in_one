import 'package:flutter/material.dart';
class AfterFront extends StatefulWidget {
  AfterFront({Key key}) : super(key: key);

  @override
  _AfterFrontState createState() => _AfterFrontState();
}

class _AfterFrontState extends State<AfterFront> {
  @override
  Widget build(BuildContext context) {
    return Container(
       child: Center(
         child: Text("nothing"),
       ),
    );
  }
}